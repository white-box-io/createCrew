import CreatorsClient from "./creators-client";

export const metadata = {
  title: "Your Creators — CreateCrew",
  description: "Browse India's top content creators. View their profiles, platforms, and open job listings.",
};

export default function CreatorsPage() {
  return <CreatorsClient />;
}
