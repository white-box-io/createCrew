import JobDetailClient from "@/components/find-work/job-detail-client";

export const metadata = {
  title: "Job Details — CreateCrew",
  description: "View job details and apply to work with India's top content creators.",
};

export default function JobDetailPage() {
  return <JobDetailClient />;
}
