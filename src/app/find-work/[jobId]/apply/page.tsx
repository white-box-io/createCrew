import ApplyClient from "@/components/find-work/apply-client";

export const metadata = {
  title: "Apply — CreateCrew",
  description: "Submit your application for this job.",
};

export default function ApplyPage() {
  return <ApplyClient />;
}
